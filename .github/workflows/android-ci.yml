name: Android CI

on:
  push:
    branches: [main, master]
    paths:
      - 'android-app/**'
      - '.github/workflows/android-ci.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'android-app/**'
      - '.github/workflows/android-ci.yml'

env:
  JAVA_VERSION: '17'

jobs:
  build-apk:
    name: Build APK
    runs-on: ubuntu-latest
    continue-on-error: true  # Kotlin compilation needs local env verification
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platforms;android-34 build-tools;34.0.0'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7

      - name: Gradle check (configuration only)
        working-directory: android-app
        run: gradle help --no-daemon 2>&1 | tail -5

      - name: Build APK
        working-directory: android-app
        run: gradle assembleDebug --no-daemon --stacktrace 2>&1 | tail -20

      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: shadowgate-android-apk-debug
          path: android-app/app/build/outputs/apk/debug/*.apk
          retention-days: 30
